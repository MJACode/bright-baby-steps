// Single source of truth for the Grace Flare Pro Terms version. Bump this when
// ProTermsPage changes materially — it is stamped onto slp_profiles and the
// signup auth metadata as acceptance evidence, so the rendered page and the
// recorded version must never drift apart.
export const PRO_TERMS_VERSION = "1.0";
